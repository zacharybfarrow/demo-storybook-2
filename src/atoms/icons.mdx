import { Meta, IconGallery, IconItem } from "@storybook/blocks";
import { ICONLOOKUP } from "../primitives/Icon/iconTypes.tsx";

<Meta title="Atoms/Icons" />

# Icons

<IconGallery>
  {Array.from(ICONLOOKUP.keys()).map((icon) => {
    const IconComponent = ICONLOOKUP.get(icon);
    return (
      <IconItem key={icon} name={icon}>
        <IconComponent />
      </IconItem>
    );
  })}
</IconGallery>
